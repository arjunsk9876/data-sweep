[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data-sweep"
version = "0.1.0"
description = "Scan a CSV, fix common data quality issues, audit for hidden entity leakage, and explain what changed."
requires-python = ">=3.9"
dependencies = [
    "pandas>=1.3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.0",
]

[project.scripts]
data-sweep = "run:main"

[tool.setuptools]
packages = ["data_sweep", "data_sweep.detectors", "data_sweep.entity_leakage"]
py-modules = ["run"]

[tool.mypy]
python_version = "3.9"
files = ["data_sweep", "run.py"]
ignore_missing_imports = true
